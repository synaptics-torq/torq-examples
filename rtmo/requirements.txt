opencv-python-headless  # cv2: image read + draw boxes/keypoints
ml_dtypes               # bf16 dtype for the transformer seam (backbone P5 -> transformer)
ai-edge-litert          # read int8 quant params (scales/zero-points) from the TFLite parts
numpy
